<div class="container text-center mt-3">
  <h1 class="mb-5">Liste des commandes</h1>
  <div class="text-center">
    <div class= "row text-center">
      <% @user.carts.each_with_index do |cart, index| %>
        <div class="card m-2 shadow-sm p-3 mb-5  rounded hoverable" style="width: 14rem;">
          <div class="card-body">
            <h4 class="card-title">Commande numéro: <%= cart.id %></h4>
            <h5>Produits :</h5>
            <% cart.items.each do |item| %>
              <h6><%= item.title %></h6>
            <% end %>
            <h6>Montant: en attente de Dimitri YOULA<%=  %></h6>
          </div>
          <%= link_to "Supprimer la commande", nil, class: "btn btn-danger", data: {confirm: "Es-tu sur?", method: "delete"} %>
        </div>
      <% end %>
    </div>
  </div>
  <h3>Modifier mon profil</h3>
  <p>
    <%= link_to "Modifier email/mot de passe", edit_user_registration_path(@user), class: "btn btn-outline-info btn-sm" %>
  </p>
</div>